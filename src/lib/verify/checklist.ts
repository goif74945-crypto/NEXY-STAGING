export const PACK_1_VERIFY_CHECKLIST = [
  'package manager install succeeds',
  'typecheck succeeds',
  'prisma client generates successfully',
  'home page renders without runtime crash',
  'required environment variables are validated at startup',
  'route error mapper returns envelope format',
] as const;
